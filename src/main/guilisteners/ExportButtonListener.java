package main.guilisteners;

/**
 * An interface that listens to actions performed on the export events button in the admin's export list of events to
 * HTML screen.
 *
 * @author Yi Tao Li
 */
public interface ExportButtonListener {
    void onExportButtonClicked();
}
